{
  "name": "@mergemail/database",
  "version": "1.0.0",
  "description": "Database models, schemas, and migrations for Universal Mail Merger SaaS.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  }
}
